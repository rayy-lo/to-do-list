export const Project = (projectName) => {
  const project = {
    projectName,
    tasks: [],
  };

  const storeProject = () => {};

  return {
    projectName,
    tasks,
  };
};

// export const storeProject
